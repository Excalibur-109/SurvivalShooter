/// summary TemplateView ///summary
public partial class TemplateView
{
	private Image _imgTestObj;
	private Button _btnTestObj;

	private  _comDropdown;

	private TextMeshProUGUI _txtText;

	private Button _btnConfirm;

	private Button _btnConfirm2;

	private TextMeshProUGUI _txtConfirm;

	private TextMeshProUGUI _txtConfirm2;



    private void CaptureComponents ()
    {
		Transform t;
		t = t.GetChild (0);
		_imgTestObj = t.GetChild (0).GetComponent<Image> ();
		_btnTestObj = t.GetChild (0).GetComponent<Button> ();

		t = t.GetChild (1);
		_comDropdown = t.GetChild (2).GetComponent<> ();

		t = t.GetChild (2);
		_txtText = t.GetChild (0).GetComponent<TextMeshProUGUI> ();

		t = t.GetChild (0);
		_btnConfirm = t.GetChild (1).GetComponent<Button> ();

		t = t.GetChild (1);
		_btnConfirm2 = t.GetChild (3).GetComponent<Button> ();

		t = t.GetChild (0);
		_txtConfirm = t.GetChild (0).GetComponent<TextMeshProUGUI> ();

		t = t.GetChild (0);
		_txtConfirm2 = t.GetChild (0).GetComponent<TextMeshProUGUI> ();


    }
}
/// summary TemplateView ///summary